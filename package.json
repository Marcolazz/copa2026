{
  "name": "copa2026-sticker-control",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.js",
    "start": "npm run dev",
    "install:all": "npm install",
    "test": "npm run test --workspace backend && npm run build --workspace frontend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
